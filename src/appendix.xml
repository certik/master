<section>
    <title>Appendix</title>
    <section>
        <title>Polar and Spherical Coordinates</title>
<p>
Polar coordinates (radial, azimuth) <m>(r,\phi)</m> are defined by
<align>
    <e>x=r\cos\phi</e>
    <e>y=r\sin\phi</e>
</align>
Spherical coordinates (radial, zenith, azimuth) <m>(\rho,\theta,\phi)</m>:
<align>
    <e>x=\rho\sin\theta\cos\phi</e>
    <e>y=\rho\sin\theta\sin\phi</e>
    <e>z=\rho\cos\theta</e>
</align>
Note: this meaning of <m>(\theta,\phi)</m> is mostly used in the USA and in many
books. In Europe people usualy use different symbols, like 
<m>(\phi,\theta)</m>, <m>(\vartheta,\varphi)</m> and others.
</p>
</section>
<section>
    <title>Delta Function</title>
<p>
Delta function <m>\delta(x)</m> is defined such that this relation holds:
<e id="deltadef">\int f(x)\delta(x-t)\d x=f(t)</e>
No such function exists, but one can find many sequences "converging" to a delta
function:
<e id="deltalim">\lim_{\alpha\to\infty}\delta_\alpha(x) = \delta(x)</e>
more precisely:
<e id="deltaprec">\lim_{\alpha\to\infty}\int f(x)\delta_\alpha(x)\d x = 
\int f(x)\lim_{\alpha\to\infty}\delta_\alpha(x)\d x = f(t)</e> 
one example of such a sequence is:
<e>\delta_\alpha(x) = {1\over\pi x}\sin(\alpha x)</e>
It's clear that <a ref="deltaprec"/> holds for any well behaved function <m>f(x)</m>.
Mathematicians like to say it's incorrect to use such a notation when in fact
the integral <a ref="deltadef"/> doesn't "exist", but they are wrong, because it is
not important if something "exist" or not, but rather if it is clear what we
mean by our notation: <a ref="deltadef"/> is a shorthand for <a
    ref="deltaprec"/> and
<a ref="deltalim"/> gets a mathematically rigorous meaning when you integrate both
sides and use <a ref="deltadef"/> to arrive at <a ref="deltaprec"/>. Thus one uses the
relations <a ref="deltadef"/>, <a ref="deltalim"/>, <a ref="deltaprec"/> to derive all
properties of the delta function. 
</p>
<p>
Let's give an example. Let <m>{\bf\hat r}</m> be the unit vector in 3D and we can label it using
spherical coordinates <m>{\bf\hat r}={\bf\hat r}(\theta,\phi)</m>. We can also
express it in cartesian coordinates as 
<m>{\bf\hat r}(\theta,\phi)=(\cos\phi\sin\theta,\sin\phi\sin\theta,\cos\theta)</m>.
</p>
<p>
<e id="deltar">f({\bf\hat r'})=\int\delta({\bf\hat r}-{\bf\hat r'})f({\bf\hat r})\,\d
{\bf\hat r}</e>
Expressing <m>f({\bf\hat r})=f(\theta,\phi)</m> as a function of <m>\theta</m> and <m>\phi</m>
we have
<e id="deltaangles">f(\theta',\phi')=\int\delta(\theta-\theta')\delta(\phi-\phi')
f(\theta,\phi)\,\d\theta\d\phi</e>
Expressing <a ref="deltar"/> in spherical coordinates we get
<e>f(\theta',\phi')=\int\delta({\bf\hat r}-{\bf\hat r'})
f(\theta,\phi)\sin\theta\,\d\theta\d\phi</e>
and comparing to <a ref="deltaangles"/> we finally get
<e>\delta({\bf\hat r}-{\bf\hat r'})={1\over\sin\theta}
\delta(\theta-\theta')\delta(\phi-\phi')</e>
In exactly the same manner we get
<e>\delta({\bf r}-{\bf r'})=\delta({\bf\hat r}-{\bf\hat r'})
{\delta(\rho-\rho')\over\rho^2}</e>
See also <a ref="functionalderdel"/> for an example of how to deal with more complex
expressions involving the delta function like <m>\delta^2(x)</m>.
</p>
</section>
<section>
    <title>Variations and functional derivatives</title>
<p>
Functional derivatives are a common source of confusion and especially the
notation. The reason is similar to the delta function --- it's due to rigid
mathematicians, who refuse to use a clear and exact notation and unfortunately
they are allowed to teach the basic calculus to physics students.
</p>
<p>
Let's have <m>\x=(x_1,x_2,\dots,x_N)</m>. The function <m>f(\x)</m> assigns a number
to each <m>\x</m>. We define a differential of <m>f</m> as
<e>\d f\equiv \left.{\d\over\d\varepsilon}f(\x+\varepsilon\h)
\right|_{\varepsilon=0}
=\lim_{\varepsilon\to0}
{f(\x+\varepsilon\h)-f(\x)\over\varepsilon}=\a\cdot\h</e>
The last equality follows from the fact, that
<m>\left.{\d\over\d\varepsilon}f(\x+\varepsilon\h) \right|_{\varepsilon=0}</m> is a
linear function of <m>\h</m>.
We define <m>{\partial f\over\partial x_i}</m> as
<e>\a\equiv\left({\partial f\over\partial x_1},{\partial f\over\partial x_2},
\dots,{\partial f\over\partial x_N}\right)</e>
This also gives a formula for computing <m>{\partial f\over\partial x_i}</m>: we
set <m>h_j=\delta_{ij}h_i</m> and
<e>{\partial f\over\partial x_i}=a_i=\a\cdot\h=\left.{\d\over\d\varepsilon}
f(\x+\varepsilon(0,0,\dots,1,\dots,0))\right|_{\varepsilon=0}=</e>
<e>=\lim_{\varepsilon\to0}
{f(x_1,x_2,\dots,x_i+\varepsilon,\dots,x_N)-f(x_1,x_2,\dots,x_i,\dots,x_N)
\over\varepsilon}</e>
But this is just the way the partial derivative is usually defined.
Every variable can be treated as a function (very simple one):
<e>x_i=g(x_1,\dots,x_N)=\delta_{ij}x_j</e>
and so we define
<e>\d x_i\equiv\d g=\d(\delta_{ij}x_j)=h_i</e>
and thus we write <m>h_i=\d x_i</m> and <m>\h=\d\x</m> and 
<e>\d f={\d f\over\d x_i}\d x_i</e>
So <m>\d\x</m> has two meanings --- it's either <m>\h=\x-\x_0</m> (a finite change in
the independent variable <m>\x</m>) or a differential,
depending on the context. Even mathematicians use this notation.
</p>
<p>
Functional <m>F[f]</m> assigns a number to each function <m>f(x)</m>. The variation is
defined as 
<e>\delta F[f]\equiv\left.{\d\over\d\varepsilon}F[f+\varepsilon h]
\right|_{\varepsilon=0}=\lim_{\epsilon\to0}{F[f+\epsilon h]-F[f]\over\epsilon}=
\int a(x)h(x)\d x</e>
We define <m>{\delta F\over\delta f(x)}</m> as
<e>a(x)\equiv{\delta F\over\delta f(x)}</e>
This also gives a formula for computing <m>{\delta F\over\delta f(x)}</m>: we
set <m>h(y)=\delta(x-y)</m> and
<e>{\delta F\over\delta f(x)}=a(x)=\int a(y)\delta(x-y)\d y=
\left.{\d\over\d\varepsilon}F[f(y)+\varepsilon\delta(x-y)]
\right|_{\varepsilon=0}=</e>
<e>=\lim_{\varepsilon\to0}
{F[f(y)+\varepsilon\delta(x-y)]-F[f(y)]\over\varepsilon}</e>
Every function can be treated as a functional (although a very simple one):
<e>f(x)=G[f]=\int f(y)\delta(x-y)\d y</e>
and so we define
<e>\delta f\equiv\delta G[f]=
\left.{\d\over\d\varepsilon}G[f(x)+\varepsilon h(x)]
\right|_{\varepsilon=0}=
\left.{\d\over\d\varepsilon}(f(x)+\varepsilon h(x))
\right|_{\varepsilon=0}=
h(x)</e>
thus we write <m>h=\delta f</m> and
<e>\delta F[f]=\int {\delta F\over\delta f(x)}\delta f(x)\d x</e>
so <m>\delta f</m> have two meanings --- it's either 
<m>h(x)=\left.{\d\over\d\varepsilon}(f(x)+\varepsilon h(x))
\right|_{\varepsilon=0}</m>
(a finite change in the
function <m>f</m>) or a variation of a functional, depending on the context.
Mathematicians never write <m>\delta f</m> in the meaning of <m>h(x)</m>, they always
write the latter, but it's ridiculous, because it is completely analogous to
<m>\d\x</m>.
</p>
<p>
The correspondence between the finite and infinite dimensional case can be
summarized as:
<align sep="\Longleftrightarrow">
    <e>f(x_i) \quad\Longleftrightarrow\quad F[f]</e>
    <e>\d f=0 \quad\Longleftrightarrow\quad \delta F=0</e>
    <e>{\partial f\over\partial x_i}=0 \quad\Longleftrightarrow\quad
        {\delta F\over\delta f(x)}=0</e>
    <e>f \quad\Longleftrightarrow\quad F</e>
    <e>x_i \quad\Longleftrightarrow\quad f(x)</e>
    <e>x \quad\Longleftrightarrow\quad f</e>
    <e>i \quad\Longleftrightarrow\quad x</e>
</align>
</p>
<p>
More generally, <m>\delta</m>-variation can by applied to any function <m>g</m> which contains
the function <m>f(x)</m> being varied, you just need to replace <m>f</m> by
<m>f+\epsilon h</m> and apply <m>{\d\over\d\epsilon}</m> to the whole <m>g</m>, for
example (here <m>g=\partial_\mu\phi</m> and <m>f=\phi</m>):
<e>\delta\partial_\mu\phi=\left.{\d\over\d\varepsilon}\partial_\mu(\phi+\varepsilon h) \right|_{\varepsilon=0}=
\partial_\mu\left.{\d\over\d\varepsilon}(\phi+\varepsilon h)
\right|_{\varepsilon=0}=\partial_\mu\delta\phi</e>
</p>
<p>
This notation allows us a very convinient computation, as shown in the
following examples. First, when computing a variation of some integral, when
can interchange <m>\delta</m> and <m>\int</m>:
<e>F[f]=\int K(x) f(x) \d x</e>
<e>\delta F=\delta \int K(x) f(x) \d x 
= \left.{\d\over\d\varepsilon}\int K(x) (f+\varepsilon h)\d
x\right|_{\varepsilon=0}=
\left.\int{\d\over\d\varepsilon} (K(x) (f+\varepsilon h))\d
x\right|_{\varepsilon=0}=</e>
<e>=\int\delta(K(x) f(x))\d x </e>
In the expression <m>\delta(K(x) f(x))</m> we must understand from the context if we
are treating it as a functional of <m>f</m> or <m>K</m>. In our case it's a functional of
<m>f</m>, so we have <m>\delta(K f)=K\delta f</m>.
</p>
<p>
A few more examples:
<e>{\delta\over\delta f(t)}\int\d t'f(t')g(t')=
\left.{\d\over\d\varepsilon}\int\d t'(f(t')+\varepsilon\delta(t-t'))g(t')
\right|_{\varepsilon=0}=g(t)</e>
<e>{\delta f(t')\over\delta f(t)}=
\left.{\d\over\d\varepsilon}(f(t')+\varepsilon\delta(t-t'))
\right|_{\varepsilon=0}=\delta(t-t')</e>
<e>{\delta f(t_1)f(t_2)\over\delta f(t)}=
\left.{\d\over\d\varepsilon}(f(t_1)+\varepsilon\delta(t-t_1))
(f(t_2)+\varepsilon\delta(t-t_2))
\right|_{\varepsilon=0}=\delta(t-t_1)f(t_2)+f(t_1)\delta(t-t_2)</e>
<e>{\delta\over\delta f(t)}\half\int\d t_1\d t_2K(t_1,t_2)f(t_1)f(t_2)=
\half\int\d t_1\d t_2K(t_1,t_2){\delta f(t_1)f(t_2)\over\delta f(t)}=</e>
<e>=\half\left(\int\d t_1 K(t_1,t)f(t_1)+\int\d t_2 K(t,t_2)f(t_2)\right)
=\int\d t_2 K(t,t_2)f(t_2)</e>
The last equality follows from <m>K(t_1,t_2)=K(t_2,t_1)</m> (any antisymmetrical
part of a <m>K</m> would not contribute to the symmetrical integration).
<e>{\delta\over\delta f(t)}\int f^3(x)\d x=
\left.{\d\over\d\varepsilon}\int(f(x)+\varepsilon\delta(x-t))^3\d x
\right|_{\varepsilon=0}=</e>
<e>=\left.\int3(f(x)+\varepsilon\delta(x-t))^2\delta(x-t)\d x
\right|_{\varepsilon=0}=\int3f^2(x)\delta(x-t)\d x=3f^2(t)</e>
Some mathematicians would say the above calculation is incorrect, because
<m>\delta^2(x-t)</m> is undefined. But that's not true, because in case of such
problems the above notation
automatically implies working with some sequence <m>\delta_\alpha(x) \to \delta(x)</m>
(for example <m>\delta_\alpha(x) = {1\over\pi x}\sin(\alpha x)</m>)
and taking the limit <m>\alpha\to\infty</m>:
<e>{\delta\over\delta f(t)}\int f^3(x)\d x=
\left.\lim_{\alpha\to\infty}{\d\over\d\varepsilon}\int(f(x)+\varepsilon\delta_\alpha(x-t))^3\d x
\right|_{\varepsilon=0}=</e>
<e>=\left.\lim_{\alpha\to\infty}\int3(f(x)+\varepsilon\delta_\alpha(x-t))^2\delta_\alpha(x-t)\d x
\right|_{\varepsilon=0}=\lim_{\alpha\to\infty}\int3f^2(x)\delta_\alpha(x-t)\d x=</e>
<e id="functionalderdel">=\int3f^2(x)\lim_{\alpha\to\infty}\delta_\alpha(x-t)\d x=
\int3f^2(x)\delta(x-t)\d x=3f^2(t)</e>
As you can see, we got the same result, with the same rigor, but using an
obfuscating notation. That's why such obvious manipulations with <m>\delta_\alpha</m>
are tacitly implied.
</p>
</section>
<section>
    <title>Spherical Harmonics</title>
<p>
Are defined by
<e>Y_{lm}(\theta,\phi)=\sqrt{{2l+1\over4\pi}{(l-m)!\over(l+m)!}}\,P_l^m(\cos\theta)\,e^{im\phi}</e>
where <m>P_l^m</m> are associated Legendre polynomials defined by
<e>P_l^m(x)=(-1)^m (1-x^2)^{m/2}{\d^m\over\d x^m} P_l(x)</e>
and <m>P_l</m> are Legendre polynomials defined by the formula
<e>P_l(x)={1\over2^l l!}{\d^l\over\d x^l}[(x^2-1)^l]</e>
they also obey the completeness relation
<e id="Lorto">\sum_{l=0}^\infty {2l+1\over2}P_l(x')P_l(x)=\delta(x-x')</e>
The spherical harmonics are ortonormal:
<e id="Yorto">\int Y_{lm}\,Y^*_{l'm'}\,\d\Omega = 
\int_0^{2\pi}\int_0^{\pi}
Y_{lm}(\theta,\phi)\,Y^*_{l'm'}(\theta,\phi)\sin\theta\,\d\theta\,\d\phi = 
\delta_{mm'}\delta_{ll'}</e>
and complete (both in the <m>l</m>-subspace and the whole space):
<e id="lcomplete">\sum_{m=-l}^l|Y_{lm}(\theta,\phi)|^2={2l+1\over4\pi}</e>
<e id="Ycomplete">\sum_{l=0}^\infty\sum_{m=-l}^lY_{lm}(\theta,\phi)Y_{lm}^*(\theta',\phi')
={1\over\sin\theta}\delta(\theta-\theta')\delta(\phi-\phi')=
\delta({\bf\hat r}-{\bf\hat r'})</e>
The relation <a ref="lcomplete"/> is a special case of an addition theorem for
spherical harmonics
<e id="lsum">\sum_{m=-l}^lY_{lm}(\theta,\phi)Y_{lm}^*(\theta',\phi')=
{4\pi\over 2l+1}P_l(\cos\gamma) 
</e>
where <m>\gamma</m> is the angle between the unit vectors given by <m>{\bf\hat
r}=(\theta,\phi)</m>
and <m>{\bf\hat r'}=(\theta',\phi')</m>:
<e>\cos\gamma=\cos\theta\cos\theta'+\sin\theta\sin\theta'\cos(\phi-\phi')
={\bf\hat r}\cdot{\bf\hat r'}</e>
</p>
</section>
<section>
    <title>Dirac Notation</title>
<p>
The Dirac notation allows a very compact and powerful way of writing equations
that describe a function expansion into a basis, both discrete (e.g. a fourier
series expansion) and continuous (e.g. a fourier transform) and related
things. The notation is designed so that it is very easy to remember and it
just guides you to write the correct equation.
</p>
<p>
Let's have a function <m>f(x)</m>. We define
<align sep="\equiv">
    <e>\braket{x|f}\equiv f(x)</e>
    <e>\braket{x'|f}\equiv f(x')</e>
    <e>\braket{x'|x}\equiv\delta(x'-x)</e>
    <e>\int\ket{x}\bra{x}\d x\equiv\one</e>
</align>
The following equation
<e>f(x')=\int\delta(x'-x)f(x)\d x</e>
then becomes
<e>\braket{x'|f}=\int\braket{x'|x}\braket{x|f}\d x</e>
and thus we can interpret <m>\ket{f}</m> as a vector, <m>\ket{x}</m> as a basis and
<m>\braket{x|f}</m> as the coefficients in the basis expansion:
<e>\ket{f}=\one\ket{f}=\int\ket{x}\bra{x}\d x\ket{f}=
\int\ket{x}\braket{x|f}\d x</e>
That's all there is to it. Take the above rules as the operational definition
of the Dirac notation. It's like with the delta function - written alone it
doesn't have any meaning, but there are clear and nonambiguous rules to convert
any expression with <m>\delta</m> to an expression which even mathematicians
understand (i.e. integrating, applying test functions and using other relations
to get rid of all <m>\delta</m> symbols in the expression -- but the result is
usually much more complicated than the original formula). It's the same with
the ket <m>\ket{f}</m>: written alone it doesn't have any meaning, but you can
always use the above rules to get an expression that make sense to everyone
(i.e. attaching any bra to the left and rewritting all brackets <m>\braket{a|b}</m>
with their equivalent expressions) -- but it will be more complex and harder to
remember and -- that is important -- less general.  
</p>
<p>
Now, let's look at the spherical harmonics:
<e>Y_{lm}({\bf\hat r})\equiv\braket{{\bf\hat r}|lm}</e>
on the unit sphere, we have
<e>\int\ket{\bf\hat r}\bra{\bf\hat r}\d{\bf\hat r}=
\int\ket{\bf\hat r}\bra{\bf\hat r}\d\Omega=\one</e>
<e>\delta({\bf\hat r}-{\bf\hat r'})=\braket{{\bf\hat r}|{\bf\hat r'}}</e>
thus
<e>\int_0^{2\pi}\int_0^{\pi}
Y_{lm}(\theta,\phi)\,Y^*_{l'm'}(\theta,\phi)\sin\theta\,\d\theta\,\d\phi = 
\int\braket{l'm'|{\bf\hat r}}\braket{{\bf\hat r}|lm}\d\Omega=
\braket{l'm'|lm}
</e>
and from <a ref="Yorto"/> we get
<e>\braket{l'm'|lm}=\delta_{mm'}\delta_{ll'}</e>
now
<e>\sum_{lm}Y_{lm}(\theta,\phi)Y_{lm}^*(\theta',\phi')=
\sum_{lm}\braket{{\bf\hat r}|lm}\braket{lm|{\bf\hat r'}}
</e>
from <a ref="Ycomplete"/> we get
<e>\sum_{lm}\braket{{\bf\hat r}|lm}\braket{lm|{\bf\hat r'}}=
\braket{{\bf\hat r}|{\bf\hat r'}}</e>
so we have
<e>\sum_{lm}\ket{lm}\bra{lm}=\one</e>
so <m>\ket{lm}</m> forms an ortonormal basis. Any function defined on the sphere
<m>f({\bf\hat r})</m> can be written using this basis:
<e>f({\bf\hat r})
=\braket{{\bf\hat r}|f}
=\sum_{lm}\braket{{\bf\hat r}|lm}\braket{lm|f}
=\sum_{lm}Y_{lm}({\bf\hat r})f_{lm}
</e>
where
<e>
f_{lm}=\braket{lm|f}=\int\braket{lm|{\bf\hat r}}\braket{{\bf\hat r}|f}\d\Omega
=\int Y_{lm}^*({\bf\hat r}) f({\bf\hat r})\d\Omega
</e>
If we have a function <m>f({\bf r})</m> in 3D, we can write it as a function of
<m>\rho</m> and <m>{\bf\hat r}</m> and expand only with respect to the variable
<m>{\bf\hat r}</m>: 
<e>f({\bf r})=f(\rho{\bf\hat r})\equiv g(\rho,{\bf\hat r})=
\sum_{lm}Y_{lm}({\bf\hat r})g_{lm}(\rho)
</e>
In Dirac notation we are doing the following: we decompose the space 
into the angular and radial part
<e>\ket{{\bf r}}=\ket{{\bf\hat r}}\otimes\ket{\rho}
\equiv\ket{{\bf\hat r}}\ket{\rho}
</e>
and write
<e>f({\bf r})=\braket{{\bf r}|f}=\bra{{\bf\hat r}}\braket{\rho|f}=
\sum_{lm}Y_{lm}({\bf\hat r})\bra{lm}\braket{\rho|f}
</e>
where
<e>\bra{lm}\braket{\rho|f}=
\int\braket{lm|{\bf\hat r}}\bra{{\bf\hat r}}\braket{\rho|f}\d\Omega
=\int Y_{lm}^*({\bf\hat r}) f({\bf r})\d\Omega
</e>
Let's calculate <m>\braket{\rho|\rho'}</m>
<e>
\braket{{\bf r}|{\bf r'}}=\bra{\bf\hat r}\braket{\rho|\rho'}\ket{{\bf\hat r'}}
=\braket{{\bf\hat r}|{\bf\hat r'}}\braket{\rho|\rho'}
</e>
so
<e>\braket{\rho|\rho'}
={\braket{{\bf r}|{\bf r'}}\over\braket{{\bf\hat r}|{\bf\hat r'}}}
={\delta(\rho-\rho')\over\rho^2}</e>
We must stress that <m>\ket{lm}</m> only acts in the <m>\ket{{\bf\hat r}}</m> space (not
the <m>\ket\rho</m> space) which means that
<e>\braket{{\bf r}|lm}
=\bra{\bf\hat r}\braket{\rho|lm}
=\braket{{\bf\hat r}|lm}\bra{\rho}
=Y_{lm}({\bf\hat r})\bra{\rho}
</e>
and <m>V\ket{lm}</m> leaves <m>V\ket\rho</m> intact. Similarly,
<e>\sum_{lm} \ket{lm}\bra{lm}=\one</e>
is a unity in the <m>\ket{\bf\hat r}</m> space only (i.e. on the unit sphere).
</p>
<p>
    Let's rewrite the equation <a ref="lsum"/>:
<e>\sum_m\braket{{\bf\hat r}|lm}\braket{lm|{\bf\hat r'}}=
{4\pi\over 2l+1} \braket{{\bf\hat r}\cdot{\bf\hat r'}|P_l}</e>
Using the completeness relation <a ref="Lorto"/>:
<e>\sum_l {2l+1\over2}\braket{x'|P_l}\braket{P_l|x}=\braket{x'|x}</e>
<e>\sum_l \ket{P_l}{2l+1\over2}\bra{P_l}=\one</e>
we can now derive a very important formula true for every function 
<m>f({\bf\hat r}\cdot{\bf\hat r'})</m>:
</p>
<p>
<e>f({\bf\hat r}\cdot{\bf\hat r'})=\braket{{\bf\hat r}\cdot{\bf\hat r'}|f}=
\sum_l \braket{{\bf\hat r}\cdot{\bf\hat r'}|P_l}{2l+1\over2}\braket{P_l|f}=
\sum_{lm}\braket{{\bf\hat r}|lm}\braket{lm|{\bf\hat r'}}{(2l+1)^2\over8\pi}
\braket{P_l|f}=
</e>
<e>=\sum_{lm}\braket{{\bf\hat r}|lm}f_l
\braket{lm|{\bf\hat r'}}
</e>
where
<e>f_l={(2l+1)^2\over8\pi}\braket{P_l|f}
={(2l+1)^2\over8\pi}\int_{-1}^1 \braket{P_l|x}\braket{x|f}\d x
={(2l+1)^2\over8\pi}\int_{-1}^1 P_l(x)f(x)\d x</e>
or written explicitly 
<e id="fylm">f({\bf\hat r}\cdot{\bf\hat r'})=
\sum_{l=0}^\infty\sum_{m=-l}^l Y_{lm}({\bf\hat r}) f_l Y_{lm}^*({\bf\hat r'})
</e>
        </p>
    </section>
</section>
